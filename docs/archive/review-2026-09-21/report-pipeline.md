# Pipeline review — 2026-09-20

Repo `/Users/brian/Repositories/websites/jastrow`, branch `review/2026-09-20-comprehensive`, HEAD `12a40a30`. Tree clean before and after (`git status --short | wc -l` = 0 both times). Every number below comes from a command quoted beside it. Raw captures in this scratchpad: `dryrun.log`, `rebuild.log`, `rebuild.diff`, `replay.log`, `migration-report.{pre,post}.json`.

## Executive summary

1. **(a) Data files — PARTIAL.** The pipeline produces all 32,512 entry files with zero composition failures and zero source-markup residue, but the tree committed at HEAD is one PR behind it: a from-scratch `--write` in a throwaway worktree changed **17 entry files and both slug-index files** (`git status --short | wc -l` = 19). #114 added 16 reviewed headword patches and regenerated the blessing doc without re-running `--write`.
2. **(b) Issue lists — PARTIAL.** `docs/v2/review-report.md` is generated and split `blocks`/`defer`/`note` (302 / 2,204 / 7), but both halves are wrong against the bar: 271 of the 300 `headword-unparsed` "blocks" are multi-word headwords flagged only for a space (all 271 contain a space; 0 do not), while the five classes step 11 ruled render-visible and blocking (342 + 85 + 88 + 33 + 22) produce **0 rows** — none has a detector on the import path. Rows are `- rid: detail` with no action: a dump grouped by kind, not a work list.
3. **(c) Sefaria corrections — NOT MET.** `docs/v2/sefaria-report.md` and `docs/v2/upstream-issues.md` are hand-written by their own declaration ("This file is **not** generated — edit by hand"); every evidence link points at archived research under `docs/archive/`, and no rule, patch or report row feeds a line. The register's footer says the counts cannot be regenerated because `body:census` was archived.
4. **Reproducibility — MET for the code, not for the committed artefact.** Dry run: `real 100.56` s, exit 0, nine gates green, `git status --short` empty afterwards (blessing and review report byte-identical; there is no timestamp line to differ). Full rebuild: byte-identical except the 19 files in (1), all attributable to #114's patches.
5. Snapshot pin is current: `data/patches/snapshot.lock` = report pin = `sha256:75bbc5ee…`; per-file shas equal `data/source/manifest.json`; `stalePins=0`.
6. Patch layer: 302 patch rows in 12 files, 291 in the apply set, 230 applied + 61 superseded, 0 drift, 0 faults. 61 carry-over patches are dead by construction and 11 more are dropped by one-record-per-rid consolidation with **no row at all**, against §4.2's "silent skipping is never allowed".
7. `bun patch:replay` exits 1 in 0.11 s before composing anything (the replay gate blocks on ~600 unresolved `needs_*` escalations); the command is unusable as shipped.
8. Headword design (`docs/v2/headword-design.md`) is design only: `grep partial|display|headwords\[` over `admin/pipeline/migrate/*.ts` and the schema finds only two unrelated comment hits. The schema still carries `headword` + `altHeadwords`; migrate emits no `partial`.
9. Smallest set before `compile.ts` starts: (i) re-run `--write`, format, commit the 19-file delta; (ii) settle the headword schema shape (not the print work); (iii) correct the publication split so `blocks` names what actually blocks. Everything else in §4 can follow compile.
10. Operability: `--write` refuses on a non-empty `data/entries` with "migration writes once" (contradicts R1); unknown flags are silently ignored (`--help` runs a 100 s dry run); `admin/pipeline/README.md` never mentions `bun install`, the mise-pinned bun 1.3.14, the `rm -rf data/entries` precondition, or `docs/v2/review-report.md`.

---

## 1. Reproducibility test

### 1.1 Dry run in the main checkout

```
$ git status --short | wc -l          → 0
$ /usr/bin/time -p bun data:import    (stdout+stderr → scratchpad/dryrun.log)
gate bodyRoundTrips=32512/32512 failures=0
gate headwordRoundTrip=76106/76106 failures=0
gate textConservation=232579/232579 failures=0
gate schema=32512/32512 failures=0
gate chain=32514/32514 failures=0
gate internalTargets=0/0 failures=0
gate slugs=32512/32512 failures=0
gate pages=32512/32512 failures=0
gate composition=65024/65024 failures=0
unresolved=0 review-deferred=3 slug-changed=7 slug-unsafe=2 page-confidence-medium=1893 page-confidence-low=298 headword-unparsed=300 markup-carry=10 orphan-ref-unbased=0
stalePins=0 upstreamFixed=0 upstreamChanged=0
slugs=regenerated slug-changed=7 slug-new=0 slug-frozen-stem-drift=0 slug-alias-new=0 slug-bare-held=0 slug-unsafe=2
real 100.56   user 101.11   sys 1.30
EXIT=0
$ git status --short                  → (empty)
$ git diff --stat                     → (empty)
```

- `docs/v2/migration-blessing.md`: **byte-identical**; there is no timestamp header (line 3 is `Generated by \`bun data:import\` over 32512 entries. Every gate is green.`).
- `docs/v2/review-report.md`: **byte-identical**.
- `data/source/migration-report.json`: gitignored (D2), so `git diff` cannot see it. I copied the pre-run file first (mtime `Sep 18 13:45`). `diff` of the `jq -S` forms: the only differences are `headwordRoundTrip` 76104 → 76106 and new `applied` outcome rows for `P000290`–`P000306` — the stale local file predates #114's patches. Not a reproducibility defect; the committed docs are the witness and they did not move.
- Restore: `git checkout -- docs/v2 data/source` → `git status --short` empty. Tree dirty for well under a minute.

### 1.2 Full rebuild control (throwaway worktree, per the coordinator's amendment)

```
$ git worktree add "$TMPDIR/rebuild-wt" HEAD && cd "$TMPDIR/rebuild-wt" && bun install   (19 packages)
$ rm -rf data/entries
$ /usr/bin/time -p bun data:import --write
  … nine gates identical to 1.1 …
  Formatted 32512 files in 1576ms. Fixed 2786 files.
  wrote 32512 slug rows and 4412 aliases
  wrote 32512 truth files under data/entries
  real 101.84   IMPORT_EXIT=0
$ bun qa:format
  Formatted 32711 files in 1192ms. No fixes applied.   FORMAT_EXIT=0
$ git status --short | wc -l       → 19
$ git diff --stat | tail -3
 data/slug-index/aliases.jsonl |  2 +-
 data/slug-index/entries.jsonl | 14 +++++++-------
 19 files changed, 53 insertions(+), 41 deletions(-)
$ git worktree remove --force "$TMPDIR/rebuild-wt"   → exit 0; main checkout still 0 dirty files
```

**Expectation was zero changed files; result is 19.** Every changed file (full diff: `scratchpad/rebuild.diff`, 348 lines):

| File | What the rebuild changed | Cause |
|---|---|---|
| `A/A01161.json`, `M/M02868.json` | alt `"אַיְידָא אֵידָא"` split into two forms | #114 reviewed `reform` patches (H6 two spellings) |
| `A/A02356.json`, `B/B00407.json`, `D/D00844.json` | `"אִסְטְוָוא  I, II,"` → `"אִסְטְוָוא I, II"` | H1 leaked comma |
| `B/B00098.json`, `B/B00443.json`, `C/C00329.json`, `M/M00447.json` | `"בַּד  V"` → `text:"בַּד", homograph:5` | H1 separator defect |
| `F/F00009.json` | `וַארְךּוּנְיָא` → `וַארְדּוּנְיָא`; **slug** `וארךוניא` → `וארדוניא` | X2 OCR glyph |
| `I/I00158.json`, `I/I00654.json`, `I/I00655.json`, `M/M02116.json` | headword split at the space; **slugs** `טוייה-טויה`→`טוייה`, `טפילה-טפילא`→`טפילה-1`, `טפילה`→`טפילה-2`, `מעיינא-מעינא`→`מעיינא` | H6 split; I00655 renumbered when a cleaned stem joined its family |
| `Q/Q00752.json`, `Q/Q00000.json` | `פִּ`+`י`+`סְחָא` → `פִּיסְחָא`; **slugs** `פ-2`→`פיסחא`, `פ-1`→`פ` | X1/X3 join; Q00000 loses its number when its family drops to one |
| `U/U01000.json` | `שִׁיפָ`+`ה` → `שִׁיפָה` | X1/X3 split word |
| `data/slug-index/entries.jsonl` (7 rows), `aliases.jsonl` (1 row) | the seven slug moves above | |

Cross-checks: the committed review report already lists exactly these seven as `slug-changed` notes (`sed -n 2539,2549p docs/v2/review-report.md`); `git log --oneline -3 -- data/entries` shows the last write was `7de656a6 (#96)`, while `data/patches/reviewed` was last touched by `12a40a30 (#114)`. #114 shipped patches plus a regenerated dry-run doc, not the write. `bun qa` cannot catch this: `validate.ts` checks entry slug against index row both ways, and both still hold the *old* value.

| sev | conf | path | claim | evidence |
|---|---|---|---|---|
| high | high | `data/entries/`, `data/slug-index/` | Committed entry data ≠ pipeline output at HEAD (17 + 2 files) | rebuild control; `slug-changed=7` in the committed report |
| info | high | `docs/v2/migration-blessing.md`, `docs/v2/review-report.md` | Dry run byte-reproducible, no timestamp noise | `git status --short` empty after run |
| low | high | `data/source/migration-report.json` | Uncommitted machine report goes stale silently; `headword-issues.ts` reads it | pre/post diff shows P000290+ absent from the Sep 18 file |

## 2. Stage map

What `admin/pipeline/migrate.ts` `main()` does, in order:

| # | Step | Function / module | Gate or rows |
|---|---|---|---|
| 0 | Parse `--write` / `--strict` | `migrate/options.ts` `runOptions` | unknown flags ignored |
| 0a | If `--write`: refuse unless `data/entries` has no `*/*.json`; resolve biome | `outputTreeIsEmpty`, `migrate/biome.ts` | throws "already holds truth files; migration writes once" |
| 1 | Compile AJV schema | `schema/entry.schema.json` | |
| 2 | **Patch preflight**: load reviewed + accepted + carry-over corpora, compute snapshot pin, `corpusPreflight` (ids, overlapping targets, pins, escalations deferred), reviewed-manifest reconciliation | `preparePatches` → `patch/apply.ts`, `patch/snapshot.ts` | **throws** (not a gate) on any preflight problem; emits `review-deferred` rows |
| 3 | **Pass 1 — compose every entry** streamed from `data/source/jastrow-dictionary.jsonl`: `text-repairs` (`body/repairs.ts` `binyan-cleanup`, then text-repair transforms) → `structural-repairs` (transforms) → `patch-apply` (reviewed → accepted → carry-over) → `buildTrace` → `evaluateRoundTrip` | `composeAll`/`composeOne` → `body/compose.ts`, `transform/run.ts` + `registry.ts` (56 rules), `patch/apply.ts`, `body/dry-run.ts`, `body/dry-run-verify.ts` | **Gate 1** bodyRoundTrips; **Gate 9** composition (patch problems; a thrown repair/transform failure → entry **dropped**); rows `patch-failed`, `composition-failed`; patch outcomes via `migrate/patches.ts`; rule counts |
| 4 | Patches whose rid never streamed | `markMissingTargets` (`migrate/patches.ts`) | Gate 9; row `patch-target-missing` |
| 5 | Orphan-ref obligations on composed entries | `checkOrphanRefs` → `migrate/orphan-refs.ts` | Gate 9; row `orphan-ref-unbased` (fault) |
| 6 | **Corpus indexes**: composed + source headword maps; forms via `decomposeForm`; slugs (`SLUGS_FROZEN=false` → `regeneratedSlugs`: `assignSlugs`, `auditAliases`, `changedSlugs` vs committed index); page index | `buildIndexes` → `migrate/cite.ts`, `headword.ts`, `slug.ts`, `slug-index.ts`, `page.ts` | **Gate 5** chain (source spellings), **Gate 7** slugs, **Gate 8** pages; rows `slug-changed`, `slug-unsafe`, `slug-bare-held`, `slug-alias-failed` (fault); under freezing `slug-new`, `slug-frozen-stem-drift`, `slug-alias-new` |
| 7 | **Pass 2 — finish every entry**: form objects, markup → closed vocabulary, cite resolution, slug + page, schema key order | `finishAll` → `migrate/finish.ts`, `headword.ts` (`reviewReason`), `markup.ts`, `cite.ts` | **Gate 2** headwordRoundTrip, **Gate 3** textConservation, **Gate 4** schema, Gate 9 (`finish-failed`); rows `headword-unparsed`, `markup-carry`, `page-confidence-low/medium`; 40 samples |
| 8 | Quarantine agreement | `gateQuarantine` → `migrate/cite.ts` | **Gate 6** internalTargets (0/0 today: quarantine empty and `unresolved=0`) |
| 9 | Stamp `publication` on every review/patch row | `migrate/publication.ts` `classifyRows` | throws on an unlisted kind |
| 10 | Write `data/source/migration-report.json`, `docs/v2/migration-blessing.md`, `docs/v2/review-report.md`; print gate lines | `migrate/report.ts`, `migrate/review-report.ts` | always, dry or write |
| 11 | `isGreen` — any red gate → **throw** "at least one gate is red; see the report" (exit 1, after the reports are on disk) | `migrate/report.ts` | |
| 12 | If `--write`: write 32,512 files, `biome format --write data/entries`, write slug index + aliases (regenerating mode only), rewrite report | `writeAll`, `formatTruth`, `slug-index.ts` | |

```mermaid
flowchart TD
  A[data/source/jastrow-dictionary.jsonl] --> C
  P[data/patches: reviewed / tranches / pilot<br/>snapshot.lock] --> B[2 · preparePatches<br/>preflight · pin · review-deferred rows]
  B -- preflight problem --> X1[throw: no gate]
  B --> C[3 · composeAll, per entry<br/>text-repairs → structural-repairs → patch-apply<br/>buildTrace · evaluateRoundTrip]
  C --> G1[Gate 1 body round-trips<br/>Gate 9 composition]
  C -- throw --> D0[entry DROPPED<br/>composition-failed row]
  C --> M[4 · markMissingTargets<br/>5 · checkOrphanRefs]
  M --> G9[Gate 9]
  M --> I[6 · buildIndexes<br/>headword maps · slugs · pages]
  SI[data/slug-index/*.jsonl] --> I
  PI[data/page-index/entries.jsonl] --> I
  I --> G578[Gate 5 chain · Gate 7 slugs · Gate 8 pages<br/>slug-* rows]
  I --> F[7 · finishAll, per entry<br/>forms · markup → he/cite · refs · slug · page]
  F --> G234[Gate 2 headword · Gate 3 text · Gate 4 schema<br/>headword-unparsed · markup-carry · page-confidence rows]
  F --> Q[8 · gateQuarantine]
  QD[data/quarantine/] --> Q
  Q --> G6[Gate 6 internal targets]
  G6 --> CL[9 · classifyRows: blocks / defer / note]
  CL --> R[10 · migration-report.json<br/>migration-blessing.md · review-report.md]
  R --> IG{11 · any gate red?}
  IG -- yes --> X2[throw, exit 1]
  IG -- no, --write --> W[12 · write data/entries<br/>biome format · slug-index · report again]
  IG -- no, dry --> END[exit 0]
```

### 2.1 Discrepancies against `docs/pipeline-flow.drawio.svg`

Labels extracted with `grep -o '<text[^>]*>[^<]*</text>' docs/pipeline-flow.drawio.svg`.

| sev | conf | claim | evidence |
|---|---|---|---|
| med | high | The diagram draws **"Update run (§3.2)"** and **"Maintenance dry run (§3.3)"** as flow boxes; neither exists in code | no merge, `writtenTree`, or scheduled-fetch code under `admin/pipeline/`; §10 lists both as "after §11" |
| med | high | Inputs drawn: `data/source/`, `data/page-index/`, `data/patches/ + data/quarantine/`. **`data/slug-index/` is absent** although `loadSlugIndex` is read on every run and `--write` rewrites it | `grep -c slug-index docs/pipeline-flow.drawio.svg` → 0 |
| low | high | "2 · Rules → 3 · Patches → 4 · Review detectors → 5 · Nine gates" reads as a stage order; in the run the patch corpus is preflighted **before** any rule (and a preflight failure throws outside the gates), gates 1/9 fire inside the compose loop, and gates 2/3/4 fire in the same loop as the review detectors | `main()` order |
| low | high | "applied · upstream-fixed → archive": nothing archives; both `upstream-*` outcomes are `blocks` rows | `publication.ts`, `patches.ts` |
| low | high | "6 · write data/entries/ then biome format" omits the slug-index write and the second report write | `writeAll` |

### 2.2 Discrepancies against `admin/pipeline/README.md`

| sev | conf | claim | evidence |
|---|---|---|---|
| med | high | Stage 2 says the dry run writes "report + docs/v2/migration-blessing.md"; **`docs/v2/review-report.md` is never mentioned** | `grep -n review-report admin/pipeline/README.md` → no hits |
| low | high | The gate list names eight for nine gates; headword round-trip (gate 2) is missing | README stage 2 |
| low | high | "Last run 2026-09-09" is stale; the committed blessing doc was regenerated by #114 on 2026-09-20 | `git show --stat HEAD` |
| low | high | README does not say `data/entries` must be empty before `--write` (spec §3 does) | `migrate.ts` `main()` |
| low | high | `patch/apply-cli.ts` docstring says it runs with `escalations: 'block'`; the code passes `'defer'` and `replayGate` blocks anyway | `apply-cli.ts` |

## 3. The three outputs

### 3(a) Data files — `data/entries/` + `data/slug-index/` (produced by `--write`)

| Question | Answer | Command |
|---|---|---|
| Complete? | 32,512 on HEAD; 32,512 written by the rebuild | `find data/entries -name '*.json' \| wc -l` → 32512; rebuild log |
| Dropped entries? | **0** today: `composition` 65024/65024, `failures=0`; no `composition-failed` row | dry-run output; `jq -r '.rows[].kind' … \| sort \| uniq -c` shows no fault kinds |
| Source markup residue? | **0 files** for each of `<span`, `<a `, `href=`, `class=`, `<br`, `&nbsp;`, `<p>` | `grep -rl -F "$p" data/entries \| wc -l` per pattern |
| Unresolved forms? | 300 headword forms flagged (3b); all 76,106 form strings byte-regenerate (gate 2) | gate output |
| Matches the pipeline? | **No** — 17 entries + 2 index files stale (§1.2) | rebuild control |

Verdict: the *pipeline* meets (a); the *committed artefact* does not until `--write` is re-run and committed. `compile.ts` consuming HEAD today would compile seven wrong slugs and 17 wrong headwords relative to the blessing doc sitting beside them.

### 3(b) Issue lists — `docs/v2/review-report.md` via `migrate/publication.ts`

Today's counts (`sed -n 1,12p docs/v2/review-report.md`, identical to the run): **blocks 302, defer 2,204, note 7; 2,513 rows**. The memory's 311/309 is pre-#114; #114's 16 patches took `headword-unparsed` 309 → 300 and left 2 `slug-unsafe`.

| Kind | Rows | `publication` | Detector | Defensible against "blocks = reader sees a defect not correctable post-go-live"? |
|---|---|---|---|---|
| `headword-unparsed` | 300 | blocks | `migrate/headword.ts` `reviewReason` via `finish.ts` | **Mostly no.** 29 "grammar did not parse" + 271 "text carries characters outside the lexical set". **All 271 contain a space; 0 do not** (`sed -n 14,314p docs/v2/review-report.md \| grep 'outside the lexical' \| sed 's/^- [A-Z][0-9]*: //; s/ — .*//' \| grep -c ' '` → 271; `grep -v ' '` → nothing). `LEXICAL` excludes U+0020 while `FORM` allows it, so a multi-word headword parses and is then flagged for the space alone. `headword-design.md` §4 rules these H6 rows legitimate (reduplication, spaced variants, 9 phrase headwords, ~267 phrase alternates that become `partial`). They render and their slug hyphen-joins already. Of the 29 real failures (`(`, `?`, `=`, `…`, `I, II` lists) the design settles every shape and its §3 says display uncertainty "does not block go-live"; `slugStem` already strips the notation, so only the 2 `=` rows (A01175, A01345, also `slug-unsafe`) still move a URL. **Defensible `blocks` from this kind: 2–29, not 300.** |
| `slug-unsafe` | 2 | blocks | `slug-index.ts` `unsafeSlugs` | Yes (URL carries `=`); fix pending in #113 |
| `upstream-fixed` / `upstream-changed` | 0 | blocks | `migrate/patches.ts` | Yes in principle (fire only on a new export) |
| `page-confidence-low` / `-medium` | 298 / 1,893 | defer | `finishAll` from page-index confidence | Yes |
| `markup-carry` | 10 | defer | `migrate/markup.ts` via `finish.ts` | Yes |
| `review-deferred` | 3 | defer | reviewed manifest `needs_human_judgment` | Yes |
| `slug-changed` | 7 | note | `slug-index.ts` `changedSlugs` | **Not a note today** — these 7 are the witness that the committed tree is stale (§1.2). Correct at write time; wrong when a dry-run doc is committed without a write. |

**The five step-11 blocking classes are not detected.** `grep -rl --include='*.ts' --exclude='*.test.ts' -F "$class" admin/pipeline`: `empty-stem-section`, `stranded-open-bracket`, `open-paren-in-rtl-span`, `superscript-subsection-contradicts-link-sub-section` appear only in rule-file *comments* (`stem-section.ts:17`, `continuation-marker.ts:11,23,58`, `italic-paren.ts:105`, `stem-label.ts:14,26`); `homograph-roman-stranded-in-definition` appears nowhere. The run's row kinds (`jq -r '.rows[].kind' … \| sort \| uniq -c`) are the seven above and nothing else. Positive control that the class is real in the committed data: `grep -rl -F '"senses": []' data/entries \| wc -l` → **342**, exactly the catalogued `corpusCount` for `empty-stem-section` (`jq 'select(.blocking==true and .route!="transform")' data/patches/patterns.jsonl` → 85 / 89 / 342 / 23 / 33). Under the maintainer's 2026-09-20 ruling the true blocks count is **≥ 570 rows that the report shows as 0**, and the 302 it does show are ~90% mislabelled. **The report's `blocks` figure is not the true blocks count.**

Is it a work list? Each row is `- rid: detail` (`review-report.ts` `section()`); no action, no link, no owner, no status, and the blessing doc's "Headword review" repeats the same 300 lines. `headword-issues.md` (a separate tool, `bun run headword:issues`) links each rid to the live app and names the shape — that is what a work list looks like, and it is not the pipeline's output.

| sev | conf | path | claim | evidence |
|---|---|---|---|---|
| high | high | `migrate/headword.ts` `LEXICAL`, `publication.ts` | 271/300 `blocks` rows are a space check mislabelled as a parse failure | counts above |
| high | high | `admin/pipeline/` (import path) | 0 detectors for the 5 ruled-blocking classes; 342 verified present | grep + control |
| med | high | `publication.ts` | `slug-changed` as `note` hides a stale committed tree | §1.2 |
| med | high | `review-report.ts` | rows carry no action or link; not a work list | `section()` |
| low | high | `gates.ts` / blessing | Gate 6 reads `0 / 0`, which `mark()`'s own comment says means "never ran"; here it means quarantine is empty and `unresolved=0` | blessing table |

### 3(c) Sefaria corrections — `docs/v2/sefaria-report.md`, `docs/v2/upstream-issues.md`

Hand-written. `upstream-issues.md` line 8: "This file is **not** generated — edit by hand." Footer: "Regenerate every count against a newer dump before filing — `bun body:dry-run` still runs; `body:census` was archived". `sefaria-report.md` has 17 sections (`grep -n '^#'`); its only "generated" mention is §15's list from the archived census. Every Evidence cell in the register points to `../archive/…` (divergence audit, body-review 01–06, body-census) — research sessions, not pipeline rows.

Which pipeline artefacts *could* feed each section today:

| Register # | Section | Pipeline data that touches it | Path to a line? |
|---|---|---|---|
| 1 | ך/ד headwords (P00855/6/60) | none — F00009 (a different ך/ד) is a reviewed patch; these three are not | no |
| 2, 3, 18 | phantom / swallowed sense markers | rules (`sense-marker`, `continuation-marker`) + seed-doc-08 patches (76 + 23 applied) | rule counts exist in the blessing doc; no mapping rule → register row |
| 5, 6, 6b, 7 | malformed / nested / slashless anchors | `cite.ts`, link rules | rule counts only |
| 9, 17 | empty / padded `binyan_form` | `repairs:binyan-cleanup` count row | count only |
| 10 | damaged sense numbers | reviewed patches | patch ids exist, no upstream flag |
| 11 | refLinks resolving to no entry | quarantine list (empty today), `unresolved=0` | the run reports 0; the register says 88 — unreconciled |
| 16 | implied sense 1 | `seed-doc-08-implied-one`, 76 applied | patch ids, no flag |
| 17 | NFC order | none (#110 unbuilt) | no |
| 4, 8, 12, 13, 14, 15 | field splits, `quotes`, refs | nothing on the import path (research censuses, archived) | no |

To generate it: (1) an `upstream` marker on whatever fixes a defect — a rule id, a patch `defect_class`, a quarantine row — so a run can list "what we corrected that Sefaria could correct too"; (2) a renderer over `patchOutcomes` + rule-count rows + quarantine emitting a per-class section with rid lists and before/after from each patch's `expected_before`/post-state; (3) the six research-only rows (#4, #8, #12–#15) either get a detector (R4) or are labelled one-time findings. None of this exists; the nearest hook is `patterns.jsonl` (`id, description, reason, round, status, corpusCount` — no upstream field).

| sev | conf | path | claim | evidence |
|---|---|---|---|---|
| high | high | `docs/v2/sefaria-report.md`, `docs/v2/upstream-issues.md` | Not generated; not derivable from a run; counts frozen at 2026-07-04 with their generator archived | file headers/footers |

## 4. Gaps to the bar

**MUST-compile** = before `compile.ts` starts; **pre-pub** = after compile can start, before v2 publishes; **post** = safely after go-live; **n/a** = not needed.

| Item (§10 or found) | State today (evidence) | Class | Why |
|---|---|---|---|
| **Stale committed tree** (found) | 19 files differ from a fresh write (§1.2) | **MUST-compile** | compile reads `data/entries`; the blessing doc beside it describes a different tree |
| **Headword work** (`headword-design.md`) | Design only: 0 implementation hits for `partial`/`display`/`headwords[`; migrate emits none of them; 16 patches shipped under the *old* shape | **MUST-compile for the schema decision**; **post** for the print work (#105–#111) | compile's validate/transform/emit and search keys hang on the form object; the design replaces it and flips ~2,474 abbreviated alternates to non-keys |
| **Publication split** (found) | 271/300 blocks mislabelled; 5 ruled classes undetected (§3b) | **MUST-compile** for the relabel (cheap); **pre-pub** for the detectors | bar (c) requires a correct must-fix-now / fix-later list; publication depends on it, compile does not |
| Update run / three-way merge (§3.2) | Not built; no `writtenTree`, no `migration-written.json` | **post** (before the first hand edit or the next fetch) | no admin tool, no hand edits; today's rebuild is `rm -rf` + `--write`, which is the update run's "fresh" branch |
| R11 atomic write + guard retirement | Not built; guard refuses with "migration writes once" | **post** | cosmetic now; ships with the update run |
| R3 resilient emit from source bytes | Not built; **0 entries dropped today** (gate 9 = 65024/65024) | **post** (before the next export) | on this snapshot nothing is lost; on a new export a drop is a red gate, so it fails loud |
| R11 index maintenance under freezing | Not built | **pre-pub** | `SLUGS_FROZEN` must not flip before it ships (§7) |
| `SLUGS_FROZEN` | `false` (`slug-index.ts:36`) | **pre-pub** (flip at publication) | correct for compile |
| Unwired judgment-class detectors | 0 on the import path | 5 blocking → **pre-pub**; 16 deferred → **post** | below |
| NFC on write (#110) | Not built; slugs already NFC-checked (`slug-index.ts:62` throws) | **post** | lossless, URL-neutral; compile can normalise keys as `cite.ts` does |
| The 5 blocking classes: reader or us? | `empty-stem-section` 342: an empty binyan heading, removable without touching a URL or sense number. `stranded-open-bracket` 85, `open-paren-in-rtl-span` 88: text edits. `superscript-subsection…` 33: link-target edit. `homograph-roman-stranded` 22: the numeral moves into the headword, but slug numbering is by rid order within a stem (S2: "not a defect — by design"), so the URL does not move | **pre-pub** under the maintainer's render-visible ruling; under the bar as stated here all five are correctable post-go-live without moving a URL, so they are candidates for `defer` — the maintainer's call, but the pipeline must *detect* them either way | |
| 11 patches silently dropped by one-record-per-rid (§5) | no row, no count | **post** | data is right (later record wins); the report is blind |
| 61 dead carry-over patches (§5) | `superseded` every run | **post** | noise only |
| `bun patch:replay` unusable (§5) | exit 1 in 0.11 s on ~600 escalations | **post** | migrate's own preflight does the work with `escalations: 'defer'` |
| Gate 6 `0/0` | quarantine empty | n/a | document it |
| 298 low-confidence pages | `defer` rows | **post** | page index is ours |

## 5. Patch layer health

`bun patch:replay` (read-only by source: composes in memory, writes nothing, `process.exit(1)` on problems):

```
$ /usr/bin/time -p bun patch:replay
corpus=291 manifest=848 applied=0 absorbed=0 carried=0 problems=1
  (corpus): unresolved needs_* records block replay — every escalated entry requires a maintainer resolution first: A00018, A00051, … D00470, K00081, R00519
real 0.11   EXIT=1
```

It never reaches an entry: `replayGate` over the full manifest (848 records) blocks on every `needs_*` escalation, which the consolidation spec deferred for the import run. The command cannot succeed until ~600 escalations are resolved; it verifies nothing today.

Corpus by directory (`wc -l` over every `patches.jsonl`) and outcome in the run's report (`jq -r '.patchOutcomes[] | "\(.patchId)\t\(.outcome)"'`, joined to a patch-id → directory map):

| Directory | Patches in file | applied | superseded | no outcome row |
|---|---|---|---|---|
| `reviewed/` (human, applies first) | 112 | 112 | 0 | 0 |
| `tranches/tranche-01` (pre-patch stage → carry-over) | 64 | 5 (carried) | 58 (absorbed) | 1 |
| `tranches/calibration-2026-09-04` | 4 | 0 | 0 | 4 |
| `tranches/batch-01-2026-09-04` | 5 | 0 | 0 | 5 |
| `tranches/batch-02…05`, `residue-01` | 4 + 3 + 3 + 2 + 3 | 3 + 3 + 3 + 2 + 3 | 0 | 1 (batch-02) |
| `tranches/seed-doc-08-implied-one` | 76 | 76 | 0 | 0 |
| `tranches/seed-doc-08-sense-runs` | 23 | 23 | 0 | 0 |
| `pilot/` (pre-patch stage → carry-over) | 3 | 0 | 3 | 0 |
| **Total** | **302** | **230** | **61** | **11** |

Header counts agree: `patches: {accepted: 113, reviewed: 112, applied: 230, absorbed: 61, carried: 5, upstreamFixed: 0, upstreamChanged: 0}`; 291 in the apply set = 302 − 11; 0 fault rows; 0 duplicate ids across directories.

- **Dead patches.** 61 carry-over rows (58 `tranche-01` + 3 `pilot`) resolve 0 times on the healed entry and are `superseded` by construction on every run — rules and repairs absorbed their defects. §4.2 admits an upstream rewrite would be indistinguishable from absorption.
- **Silently dropped.** 11 patches (`P000009`, `P000068`–`P000076`, `P000080`) on four rids (A00339 ×5, A00337 ×2, A00878 ×3, A00074 ×1) have **no outcome row and no header count**. Each rid has records in 3–4 tranches (`grep -l "\"rid\":\"$rid\"" data/patches/tranches/*/manifest.jsonl`); one-record-per-rid consolidation keeps the latest and drops the rest without a row — "silent skipping is never allowed" (§4.2) is not honoured here.
- **Snapshot pin.** `data/patches/snapshot.lock` combined `sha256:75bbc5ee7ab8…` = report `snapshot.pin`; its per-file lines (`4c64ff03…` dictionary, `629e16ad…` lexicons) equal `data/source/manifest.json` `outputs[].sha256`. `stalePins=0`.
- **Is the split meaningful?** `reviewed/` is a real category (human-authored, byte-adding allowed, applies first — `reviewed/README.md`). `tranches/` is a real production input whose layout is history: ingest order and stage per directory are hard-coded in `apply.ts` `TRANCHES` (an unnamed directory is an error), the names encode sweep dates and archived seeders, and every directory carries `verdicts-*`/`rejects.jsonl` research files beside the two the loader reads. `pilot/` is history in production clothing: its 3 patches are all superseded, its 210-row manifest, `report.md` and `verdicts-*` are research records, and it contributes 0 applied patches. The split that matters to the run is *stage* (`healed` vs `pre-patch`) and *author* (human vs agent); the directory tree states neither.

| sev | conf | path | claim | evidence |
|---|---|---|---|---|
| med | high | `patch/apply-cli.ts` | `patch:replay` cannot complete on the committed corpus | exit 1 in 0.11 s, `applied=0` |
| med | high | `patch/apply.ts` consolidation | 11 patches dropped with no row or count | join above |
| low | high | `tranches/tranche-01`, `pilot/` | 61 patches dead by construction | 61 `superseded` |
| info | high | `data/patches/snapshot.lock` | pin current | sha comparison |

## 6. Operability

Following `admin/pipeline/README.md` literally as a new contributor:

| Step | Works from the README alone? | Evidence |
|---|---|---|
| Toolchain | **No mention** of `bun install`, mise, or the pinned `bun = "1.3.14"` / `biome = "2.5.2"` (`.mise.toml`). Root `README.md` has no pipeline references (`grep -n -i 'data:import\|admin/pipeline\|bun install' README.md` → 0); `CONTRIBUTING.md` mentions the pipeline only for `transform:invariants` | greps; the rebuild worktree needed `bun install` (19 packages) before `--write` could find biome |
| `bun data:fetch --cached` | `.cache/sefaria/` exists on this machine (`lexicon_entry.bson` 125 MB, `word_form.bson` 228 MB, Jul 4) but is **gitignored**, so a fresh clone has no cache and must stream the 2.4 GB dump. Missing-cache error is helpful: "`--cached given but ${path} is missing; run without --cached first`" | `ls .cache/sefaria`; `.gitignore` `.cache/`; `fetch.ts:90`. Not run (it rewrites `data/source/` or downloads) |
| `bun data:import` (dry) | Yes, with **undocumented preconditions**: `data/page-index/entries.jsonl` (`loadPageIndex`; unhandled read error if absent), `data/slug-index/{entries,aliases}.jsonl` (read even while unfrozen, for `slug-changed`), `data/patches/` directories exactly matching `apply.ts` `TRANCHES`, `data/quarantine/`. `data/entries` may be absent or full for a dry run | module constants in §2 |
| `bun data:import --write` | **Undocumented in the README: `data/entries` must be empty** (`rm -rf data/entries` first; spec §3 says it, README stage 2 does not). Refusal: "`data/entries already holds truth files; migration writes once`" — wrong under R1 and alarming to a newcomer. Biome is resolved before composing, so a missing `node_modules` fails in seconds rather than after 100 s | `migrate.ts` `main()`; rebuild log |
| Flags | `options.ts` reads exactly `--write` and `--strict` (`strict` → `pins: 'block'`, `drift: 'problem'`); **every other argument is silently ignored**, so `bun data:import --help` or a typo runs a full 100 s dry run. `fetch.ts` reads only `--cached`. README documents `--write`, `--strict`, `--cached` — matches | `options.ts`, `fetch.ts:172` |
| Failure messages | Patch preflight lists every problem with patch id (good). Red gate: "`at least one gate is red; see the report`" after the three report paths and per-gate `failures=N` lines (adequate; failure *lines* are only in the blessing doc). Gate 6's `0/0` is explained nowhere a reader sees | dry-run stdout |
| Run time | 100.6 s dry; 101.8 s write + 1.6 s format + 1.2 s `qa:format` | timings above |

| sev | conf | path | claim | evidence |
|---|---|---|---|---|
| med | high | `admin/pipeline/README.md` | Missing: `bun install`/mise, empty-tree precondition, `review-report.md`, gate 2, stale "last run" | greps |
| low | high | `migrate/options.ts` | Unknown flags silently ignored | code |
| low | high | `migrate.ts` | "migration writes once" refusal contradicts R1 | code |

---

## What must happen before `compile.ts` starts (smallest set)

1. **Re-run the write and commit it.** `rm -rf data/entries && bun data:import --write && bun qa:format`; commit the 19-file delta (17 entries + `data/slug-index/{entries,aliases}.jsonl`) with the blessing doc it belongs to. Control: a following dry run reports `slug-changed=0` and `git status` is empty. Until this lands, compile would read a tree that disagrees with its own evidence doc.
2. **Settle the headword schema shape.** Decide whether entry data keeps `headword`/`altHeadwords` or moves to `headwords[]` + `display` + `partial` (headword-design §2) *before* compile's validate/transform/emit and search index are written against the form object. This is the decision, not the print work (#105–#111). If the answer is "ship today's shape," write that down and version the design.
3. **Make the split honest.** (a) Give the 271 space-only rows their own kind (or stop `LEXICAL` complaining about U+0020) so `blocks` stops carrying them; (b) reclassify `slug-changed` so a dry-run doc committed without a write is not a `note`; (c) have the report state that the five ruled-blocking classes are undetected, with their catalogued counts, until detectors exist. Hours, not days; without them the pipeline's must-fix-now list is wrong in both directions.

## Before publication, after compile can start

- Detectors for the five ruled-blocking classes (`empty-stem-section` first: `stems[].senses == []` reproduces 342 exactly), each emitting a `blocks` row — or a maintainer ruling that under the "correctable post-go-live without moving a URL" bar they `defer`; either way the report must show them.
- R11 index maintenance, then flip `SLUGS_FROZEN` at publication (§7 forbids the flip before maintenance ships).
- #113's five headword rows (A01175, A01345, V00518, S01780, U00489) — the only rows that still move a URL.

## Safely post-go-live

- Update run: §3.2 three-way merge, `writtenTree`/`migration-written.json`, R11 atomic write and retirement of the empty-tree guard and its "writes once" message. Needed before the first admin-tool hand edit or the next `data:fetch`, whichever comes first.
- R3 resilient emit from source bytes (0 entries dropped today; a drop on a new export is already a red gate).
- Port the 16 deferred judgment-class detectors and the "no new defect" checks from `retired-corpus-checks.md`, one per PR.
- NFC on write (#110); slugs are already NFC-checked.
- A generated Sefaria register: an `upstream` marker on rules / patch classes / quarantine rows and a renderer over `patchOutcomes` + rule counts; label the six research-only sections as one-time findings or give them detectors.
- Fix or delete `bun patch:replay`; emit a row for the 11 consolidation-dropped patches; archive the 61 dead carry-over rows.
- README: `bun install`/mise, the empty-tree precondition, `review-report.md`, gate 2, "last run" date; reject unknown CLI flags; explain gate 6's `0/0`.
- Headword print work #105–#111; 298 low-confidence page placements; update `docs/pipeline-flow.drawio.svg` (add `data/slug-index/`, mark the update/maintenance runs as unbuilt).
