{
  "name": "jastrow-admin",
  "private": true,
  "type": "module",
  "description": "Local-only admin server for the Jastrow Dictionary PWA. Includes the Rabbinic Time PDF builder.",
  "scripts": {
    "start": "bun server.ts",
    "stage": "bun pdf-builds/stage.ts",
    "postinstall": "bun pdf-builds/stage.ts && bun x playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.49.0",
    "chart.js": "4.4.1",
    "@fontsource/atkinson-hyperlegible-next": "^5.0.0",
    "@fontsource/lexend": "^5.0.0",
    "@fontsource/frank-ruhl-libre": "^5.0.0"
  },
  "comment": "chart.js version is pinned to match the CDN <script> in ../../index.html. If you bump one, bump the other."
}
