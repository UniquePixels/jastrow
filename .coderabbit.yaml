# CodeRabbit Configuration
# https://docs.coderabbit.ai/guides/configure-coderabbit

language: en-US
early_access: true
reviews:
  profile: assertive
  request_changes_workflow: true
  auto_title_instructions: |
    Use the format: <emoji> <type>(scope): description
    Types and emojis: new 🦄 / improve 🌈 / fix 🦠 / chore 🧺 / release 🚀 / doc 📖 / ci 🚦
    Scope should reflect the primary area changed (e.g. ui, data, admin, ci).
    Description must be lowercase, imperative mood, max 50 chars total.
  review_details: true
  fail_commit_status: true
  suggested_labels: false
  poem: false
  pre_merge_checks:
    docstrings:
      mode: error

  auto_review:
    enabled: true
    drafts: false
    base_branches:
      - main
      - v2

  path_instructions:
    - path: "**"
      instructions: |
        General project rules:
        - Repo layout (v2): `app/` public app, `admin/` tooling (incl. `admin/pipeline/`), `data/` data only.
        - `app/` is a placeholder: the v2 frontend has not been written yet.
        - Everything running today is Bun tooling under `admin/`.
        - Biome enforces lint and formatting.

    - path: "admin/**"
      instructions: |
        Maintainer tooling that runs on Bun (not deployed). Includes:
        - `admin/pipeline/` — data pipeline from the Sefaria source (fetch, transform, validate, emit)
        - Admin tool v2 (Phase 3) will live here too
        - `console` and sequential awaits over streams are expected here

    - path: ".github/**"
      instructions: |
        CI/CD configuration. Check for:
        - Pinned action versions with hash comments
        - Harden Runner with egress blocking on all jobs
        - No credential or secret exposure risks
        - Correct branch targeting (main, plus v2 during the v2 overhaul)

issue_enrichment:
  auto_enrich:
    enabled: true
  planning:
    auto_planning:
      labels:
        - '!no-plan'
